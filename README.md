# Scripts for optimized images

- `yarn run imagemin` for optimized images with imagemin algorithm
- `yarn run webp` for optimized images using format that  google provider 
- `yarn run favicon` for become .png to .ico

